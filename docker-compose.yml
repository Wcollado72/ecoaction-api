version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: ecoaction_mysql
    restart: always
    environment:
      MYSQL_DATABASE: ecoaction_db
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3307:3306"